[build]
  command = "npm run build"
  publish = "dist"

# Proxy Groq API calls
[[redirects]]
  from = "/groq/*"
  to = "https://api.groq.com/:splat"
  status = 200
  force = true

# SPA fallback — must be last
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
